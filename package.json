{
  "name": "resume-buddy",
  "private": true,
  "version": "1.0.0",
  "description": "AI-powered career decision engine that helps students and freshers evaluate jobs and internships before applying.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dev-rohit-gupta/resume-buddy.git"
  },
  "bugs": {
    "url": "https://github.com/dev-rohit-gupta/resume-buddy/issues"
  },
  "scripts": {
    "dev": "pnpm --filter @resume-buddy/server dev",
    "dev:css": "pnpm --filter @resume-buddy/server watch:css",
    "build": "pnpm --filter @resume-buddy/server build:css && pnpm exec turbo run build",
    "build:css": "pnpm --filter @resume-buddy/server build:css",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.7.4",
    "typescript": "^5.5.0"
  },
  "packageManager": "pnpm@10.28.2",
  "homepage": "https://github.com/dev-rohit-gupta/resume-buddy#readme"
}
